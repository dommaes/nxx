<% if(dsl === 'groovy') { %>final<% } else { %>val<% } %> projectDir = project.path.substring(1).replace(<%- quote %>:<%- quote %>, <%- quote %>/<%- quote %>)
buildDir = rootProject.buildDir.toPath().resolve(projectDir).toFile()
<% if(dsl === 'groovy') { %>final<% } else { %>val<% } %> coverageDir = rootProject.file("coverage/$projectDir")

dependencyLocking {
    lockAllConfigurations()
}
